version: 0.2
env:
  shell: powershell.exe
  NODE_OPTIONS: --max-old-space-size=8192
phases:
  build:
    commands:
      - source ./shared-scripts.sh && _testLinux
artifacts:
  files:
    - 'shared-scripts.sh'
